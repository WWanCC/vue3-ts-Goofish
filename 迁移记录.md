# 前端迁移记录

## 目标

将原本位于仓库根目录的 Vue 3 + Vite 前端整理为 pnpm workspace 中的独立 Web 应用：`apps/web`。

本次仅迁移前端工程，保留 Axios 与 `vite-plugin-mock-dev-server` 的本地商品数据方案；未接入后端、Hono 或 RPC。

## 目录移动

以下源码由根目录 `src/` 移至 `apps/web/src/`：

- 应用入口：`main.ts`、`App.vue`
- 路由：`router/`
- 布局组件：`components/`
- 首页模块：`modules/home/`
- 通用 API、HTTP 工具：`shared/`
- 全局样式：`assets/main.css`

以下与 Vite Web 应用绑定的文件由根目录移至 `apps/web/`：

- `index.html`
- `public/`
- `mock/`
- `.env.development`、`env.d.ts`
- `vite.config.ts`、`vitest.config.ts`
- `tsconfig.json`、`tsconfig.app.json`、`tsconfig.node.json`、`tsconfig.vitest.json`
- `eslint.config.ts`
- 自动导入声明：`auto-imports.d.ts`、`components.d.ts`
- 前端依赖清单：`apps/web/package.json`

## Workspace 配置

新增根目录 `pnpm-workspace.yaml`，将 `apps/*` 识别为工作区包。

根目录 `package.json` 改为工作区编排入口，相关命令通过 `@goofish/web` 执行：

```sh
pnpm dev
pnpm build
pnpm preview
pnpm type-check
pnpm test:unit
pnpm lint
pnpm format
```

前端依赖、构建脚本和开发脚本均归属 `apps/web/package.json`。已重新执行 `pnpm install` 更新根目录 `pnpm-lock.yaml`。

## 路径与配置修复

### Vite 入口

`apps/web/index.html` 的模块入口设置为：

```html
<script type="module" src="/src/main.ts"></script>
```

不能写成 `/apps/web/src/main.ts`。Vite 从 `apps/web` 作为应用根目录启动时，`/src/main.ts` 会正确解析为 `apps/web/src/main.ts`；使用仓库路径会导致 `Failed to load url /apps/web/src/main.ts`。

### TypeScript 配置

`apps/web/tsconfig.app.json` 和 `apps/web/tsconfig.vitest.json` 改为应用内相对路径：

```json
{
  "include": ["env.d.ts", "src/**/*", "src/**/*.vue"],
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

避免继续使用 `apps/web/src/**/*` 这类相对于 `apps/web` 的错误嵌套路径。

### 模块别名与路由

`apps/web/vite.config.ts` 中的 `@` 保持映射到 `apps/web/src`：

```ts
'@': fileURLToPath(new URL('./src', import.meta.url))
```

路由和首页 API 的导入改为 `@/modules/...`、`@/shared/...`，不再使用 `apps/web/src/...` 作为模块路径。

### Vite Mock

保留 `vite-plugin-mock-dev-server`，由 `apps/web/vite.config.ts` 加载 `apps/web/mock/` 中的 Mock 文件。

`apps/web/mock/home.mock.ts` 的类型导入修复为：

```ts
import type { ProductItem } from '../src/modules/home/types'
```

这样 Mock 编译时可以找到迁移后的类型文件。

### 自动组件声明

`apps/web/components.d.ts` 中 `AppHeader` 的类型路径修复为：

```ts
AppHeader: typeof import('./src/components/layout/AppHeader.vue')['default']
```

### Tailwind 样式入口

`apps/web/src/assets/main.css` 恢复为 Tailwind v4 正确入口：

```css
@import 'tailwindcss';
```

迁移中该文件曾错误引用 `tailwindcss/dist/lib.d.mts`。这是 TypeScript 类型声明，不是 CSS；会使 Tailwind 构建报 `CssSyntaxError: Invalid declaration`。

## 验证结果

已在仓库根目录验证：

```sh
pnpm install
pnpm type-check
pnpm build
pnpm dev
```

结果：

- `pnpm type-check` 通过。
- `pnpm build` 通过，构建产物输出到 `apps/web/dist`。
- `pnpm dev` 可启动 Vite 开发服务器，地址为 `http://localhost:5173/`。
- `GET /api/home/products?limit=1` 由 `apps/web/mock/home.mock.ts` 正常响应商品分页数据。
